<!DOCTYPE html>
<html lang="en">
<head>

<style type="text/css">
   body { background: lightgrey !important; } 
</style>

<style>


h1{
  font-size: 72px;
  color: blue;
  text-align: center;
  font-family: Arial, Helvetica, sans-serif;
}

h2{
  font-size: 40px;
  color: darkblue;
  text-align: left;
  font-family: Arial, Helvetica, sans-serif;
}

th{
  font-size: 24px;
  color: black;
  text-align: center;
  font-family: Arial, Helvetica, sans-serif;
}

.body{
  font-family: Arial, Helvetica, sans-serif;
  text-align: center;
}

td{
  font-size: 18px;
  color: black;
  text-align: center;
  font-family: Arial, Helvetica, sans-serif;
}

</style>

<br>

<h1> hydra </h1>

<p id="notice"><%= notice %></p>

<div class="body">
<h2>Water Intake</h2>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Unit</th>
      <th>Amount</th>
      <th>Time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @waters.each do |water| %>
      <tr>
        <td><%= water.title %></td>
        <td><%= water.unit %></td>
        <td><%= water.amount %></td>
        <td><%= water.time %></td>
        <td><button type="button" class="btn btn-info"><%= link_to 'Show', water %></button></td>
        <% if user_signed_in? %>
        <td><button type="button" class="btn btn-warning"><%= link_to 'Edit', edit_water_path(water) %></button></td>
        <td><button type="button" class="btn btn-danger"><%= link_to 'Destroy', water, method: :delete, data: { confirm: 'Are you sure?' } %></button></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>
<% if user_signed_in? %>
<button type="button" class="btn btn-info"><%= link_to 'New Water', new_water_path %></button>
<% end %> 

</div>

</body>
</html>